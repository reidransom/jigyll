#!/bin/bash -eu

ddir=/tmp/$USER/jigyll-compare
basename=$(basename $1)

./scripts/jigyll build -s $1 -d $ddir/$basename.go
(cd $1 && bundle exec jekyll build -d $ddir/$basename.jekyll)
ksdiff $ddir/$basename.jekyll $ddir/$basename.go
